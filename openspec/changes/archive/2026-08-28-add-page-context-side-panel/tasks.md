# Tasks

- [x] 1.1 Add a `GET_PAGE_CONTEXT` handler to the content script that reuses `extractPageContent`
- [x] 1.2 Add an "Ask about this page" context menu item that opens the side panel synchronously on click
- [x] 1.3 Add a side panel button to the popup, resolving the tab id on mount so the click stays synchronous
- [x] 1.4 Add page-context state, fetch, and tab-change refresh to the side panel
- [x] 1.5 Render the context chip with an include control, and style it with the existing appearance tokens
- [x] 1.6 Prepend the page as a single system message ahead of the first user turn
- [x] 1.7 Cover the behaviour in the browser suite
- [x] 1.8 Full unit suite, full browser suite, and `node pack.mjs` green
